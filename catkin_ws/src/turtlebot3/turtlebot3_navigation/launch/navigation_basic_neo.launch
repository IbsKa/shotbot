<?xml version="1.0"?>

<launch>
  
  <include file="$(find turtlebot3_navigation)/configs/neo_localization/neo_localization.launch"/>
  <include file="$(find turtlebot3_navigation)/configs/move_base/move_base.launch"/>

  <!-- Arguments -->
  <arg name="model" default="$(env TURTLEBOT3_MODEL)" doc="model type [burger, waffle, waffle_pi]"/>
  <arg name="scan_topic"     default="scan_filtered"/>

  <!-- Turtlebot3  -->
  <include file="$(find turtlebot3_bringup)/launch/turtlebot3_remote.launch">
    <arg name="model" value="$(arg model)" />
  </include>

  <!-- rviz -->
  <node pkg="rviz" type="rviz" name="rviz" args="-d $(find turtlebot3_navigation)/rviz/turtlebot3_navigation.rviz"/>

  <node pkg="laser_filters" type="scan_to_scan_filter_chain" output="screen" name="shotbot_laser_filter">
      <rosparam command="load" file="$(find laser_filters)/shotbot_laser_filter/shotbot_laser_config.yaml" />
      <param name="use_hack" value="false"/>
  </node>

</launch>
